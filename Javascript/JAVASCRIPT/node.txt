Johny-Five - library for robots

node script.js

globalThis - it points to window object

globalThis === window       #it works outside browser

const new = require('file path')        # to import module
module.export ={
    new : new
}

To use import from      # name file script.js to script.mjs

npm init -y         #to create package.json
npm install nodemon --save-dev      #-dev to add into dev dependency
npm install express

Node file system module - 

app.use(express.urlencoded({extended: false}));
app.use(express.json());

process.env

https://itnext.io/how-to-use-environment-variables-in-node-js-cb2ef0e9574a

npm install dotenv
require('dotenv').config({path: __dirname + '/.env'})

